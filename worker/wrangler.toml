name = "campus-meal-pick"
main = "src/index.js"
compatibility_date = "2024-01-01"

[[kv_namespaces]]
binding = "SUBSCRIBERS"
id = "a91177b8a342487b98e30a68ac0adeda"

# Non-secret config (available in env automatically)
[vars]
GH_OWNER = "Shengle-Dai"
GH_REPO = "Campus-Meal-Pick"

# Secrets (set via `wrangler secret put` for production,
# or add to .dev.vars for local dev):
# HMAC_SECRET, GH_PAT_TOKEN
